# Copy to .env for local development only. Do not commit real keys.
GROQ_API_KEY=
OPENAI_API_KEY=

# Optional storage overrides for future deployment work.
APP_DB_PATH=./data/app.db
CHROMA_DB_PATH=./data/chroma
