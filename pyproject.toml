[project]
name = "business-rule-ai"
version = "0.1.0"
description = "React and Python platform for strict business-rule validation and corrected-file exports."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "groq>=0.4.0",
    "openai>=1.10.0",
    "chromadb>=0.4.0",
    "sentence-transformers>=2.3.0",
    "pypdf>=4.0.0",
    "python-docx>=1.1.0",
    "openpyxl>=3.1.0",
    "xlrd>=2.0.0",
    "reportlab>=4.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
